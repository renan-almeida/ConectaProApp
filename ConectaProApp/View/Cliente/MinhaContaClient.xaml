<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConectaProApp.View.Cliente.MinhaContaClient"
             BackgroundColor="#f1f1f1">

    <ScrollView>
        <VerticalStackLayout>
            <!-- Cabeçalho Azul -->
            <Grid BackgroundColor="#0f5cc2" HeightRequest="58" Padding="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image Source="conectapro_logo.png" HeightRequest="40" VerticalOptions="Center" Grid.Column="0" />
            </Grid>

            <!-- Banner/Header -->
            <!-- Banner/Header -->
            <Grid HeightRequest="210">

                <!-- Header com clique separado -->
                <Grid>
                    <Image x:Name="headerClienteImage"
               Source="{Binding FotoVMHeader.FonteImagem}"
               Aspect="AspectFill"
               HeightRequest="150"
               HorizontalOptions="Fill"
               VerticalOptions="Start" />

                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding FotoVMHeader.SelecionarFotoCommand}" />
                    </Grid.GestureRecognizers>
                </Grid>

                <!-- Avatar circular sobreposto -->
                <Grid HeightRequest="120" WidthRequest="120"
          VerticalOptions="Start"
          HorizontalOptions="Center"
          Margin="0,120,0,0"
          ZIndex="10"
          IsClippedToBounds="True">

                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SolicitacaoVM.FotoVM.SelecionarFotoCommand}" />
                    </Grid.GestureRecognizers>

                    <!-- Círculo de recorte -->
                    <Grid.Clip>
                        <EllipseGeometry Center="60,60" RadiusX="60" RadiusY="60" />
                    </Grid.Clip>

                    <!-- Imagem de perfil (avatar) -->
                    <Image x:Name="avatarImageClient"
               Source="{Binding SolicitacaoVM.FotoVM.FonteImagem}"
               WidthRequest="120"
               HeightRequest="120"
               Aspect="AspectFill"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
                </Grid>
            </Grid>

            <!-- Nome e descrição -->
            <VerticalStackLayout Padding="20,0" Spacing="5" HorizontalOptions="Center">
                <Entry x:Name="NomeEntry"
                       TextColor="Black"
                       Placeholder="Nome do Cliente"
                       FontAttributes="Bold"
                       FontSize="20"
                       Margin="20"
                       HorizontalTextAlignment="Center"
                       WidthRequest="250"
                       TextChanged="OnNomeChanged" />
                <Editor x:Name="DescricaoEditor"
                        TextColor="Black"
                        Placeholder="Sobre o cliente"
                        FontSize="14"
                        AutoSize="TextChanges"
                        WidthRequest="250"
                        TextChanged="OnDescricaoChanged"
                        Margin="0,0,0,10"
                        HorizontalOptions="Center" />
            </VerticalStackLayout>

            <!-- Abas -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="18" Padding="10">
                <Button Text="Solicitações" BackgroundColor="#0f5cc2" TextColor="White"
                        CornerRadius="20" WidthRequest="117"
                        Command="{Binding SelecionarAbaCommand}" CommandParameter="SolicitacoesCliente" />
                <Button Text="Propostas" BackgroundColor="#0f5cc2" TextColor="White"
                        CornerRadius="20" WidthRequest="117"
                        Command="{Binding SelecionarAbaCommand}" CommandParameter="PropostasCliente" />
                <Button Text="Histórico" BackgroundColor="#0f5cc2" TextColor="White"
                        CornerRadius="20" WidthRequest="117"
                        Command="{Binding SelecionarAbaCommand}" CommandParameter="HistoricoCliente" />
            </HorizontalStackLayout>

            <!-- Listas -->
            <CollectionView ItemsSource="{Binding SolicitacaoVM.SolicitacoesRecebidas}"
                            IsVisible="{Binding SolicitacaoVM.SolicitacoesClienteVisivel}" />

            <CollectionView ItemsSource="{Binding SolicitacaoVM.PropostasCliente}"
                            Margin="15,10"
                            IsVisible="{Binding SolicitacaoVM.PropostasClienteVisivel}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="10" BorderColor="Gray" Padding="10" Margin="0,5">
                            <VerticalStackLayout>
                                <Label Text="{Binding Descricao}" FontAttributes="Bold" FontSize="16" />
                                <Label Text="{Binding ValorProposto, StringFormat='Valor: R$ {0:F2}'}" FontSize="14" />
                                <HorizontalStackLayout HorizontalOptions="End">
                                    <Button Text="ACEITAR" Command="{Binding BindingContext.SolicitacaoVM.AceitarSolicitacaoCommand, Source={x:Reference Name=PropostasClienteView}}" CommandParameter="{Binding IdSolicitacao}" />
                                    <Button Text="RECUSAR" Command="{Binding BindingContext.SolicitacaoVM.RecusarSolicitacaoCommand, Source={x:Reference Name=PropostasClienteView}}" CommandParameter="{Binding IdSolicitacao}" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Lista de Histórico -->
            <CollectionView ItemsSource="{Binding SolicitacaoVM.HistoricoCliente}"
                            Margin="10"
                            SelectionMode="None"
                            IsVisible="{Binding SolicitacaoVM.HistoricoClienteVisivel}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,5" Padding="10" BackgroundColor="#f9f9f9" CornerRadius="10" BorderColor="#e0e0e0" HasShadow="True">
                            <StackLayout Spacing="5">
                                <Label Text="{Binding TituloServico}" FontAttributes="Bold" FontSize="Medium" TextColor="#333" />
                                <Label Text="{Binding DataInclusaoFormatada}" FontSize="Small" TextColor="Gray" />
                                <Label Text="{Binding ValorContratacaoFormatado, StringFormat='Valor: R$ {0}'}" FontSize="Small" TextColor="Black" />
                                <Frame BackgroundColor="#e6f0ff" Padding="5,2" CornerRadius="5" HorizontalOptions="Start">
                                    <Label Text="{Binding StatusAmigavel}" FontSize="Small" TextColor="#1e88e5" FontAttributes="Bold" />
                                </Frame>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
