<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConectaProApp.View.EsqueceuSenha.PasswordEmail"
             BackgroundColor="#F3F3F3">
    
    <VerticalStackLayout
       HorizontalOptions="Center"
       VerticalOptions="Center"
       Spacing="10">
        <Image Source="senha.png"
               HeightRequest="186"
               WidthRequest="175"
                Aspect="AspectFit"
                HorizontalOptions="Center"
                VerticalOptions="Center"/>
        
        <Label Text="Ops..." FontSize="24" HorizontalOptions="Center"
               TextColor="Black"/>
        <Label Text="Perdeu a senha? Vamos recuperar ela rapidinho!"
               FontSize="14"
               HorizontalOptions="Center"
               TextColor="Black"/>
        <Entry Placeholder="Digite o seu email ConectaPro" FontSize="15" HorizontalOptions="Fill" Text="{Binding Email}"/>
        <Button Text="Solicitar código" Command="{Binding SolicitarCodigoCommand}" FontSize="20" BackgroundColor="#0f5cc2" TextColor="White" HeightRequest="48" HorizontalOptions="Fill"/>
    </VerticalStackLayout>
</ContentPage>